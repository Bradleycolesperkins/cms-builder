  {% if block.image|length > 0 %}
      {% set imageUrl = block.image.first().getUrl() %}
  {% else %}
      {% set imageUrl = '/img/bg/bg-image-16.jpg' %}
  {% endif %}

 <img src="{{ imageUrl }}"  alt="{{ block.image.imageAltTag  }}"
      style="{% include '/template_2/_components/_extras/_image_extras' with { 'block' : image_block} %}"
 >