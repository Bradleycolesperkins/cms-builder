{% if carousel %}
    {% if carousel.slidesToShow %}
        {% set slidesToShow = carousel.slidesToShow %}
    {% else %}
        {% set slidesToShow = 3 %}
    {% endif %}
    {% if carousel.slidesToScroll %}
        {% set slidesToScroll = carousel.slidesToScroll %}
    {% else %}
        {% set slidesToScroll = 3 %}
    {% endif %}
    <!-- Start Carousel Slider -->
    <div class="bk-carousel-slider bg_color--1 ptb--40 slick-arrow-hover ptb-md--30 ptb-sm--20
    {{ carousel.elementClass }}" id="{{ carousel.elementId }}"
         style="{% include '/template_2/_components/_extras/_element_extras' with { 'block' : carousel} %}"
    >
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <div class="brook-element-carousel slick-arrow-center" data-slick-options='{
                                "spaceBetween": 15,
                                "slidesToShow": {{ slidesToShow }},
                                "slidesToScroll": {{ slidesToScroll }},
                                "arrows": true,
                                "infinite": true,
                                "dots": false,
                                "autoplay": true,
                                "autoplaySpeed": 2000,
                                "prevArrow": {"buttonClass": "slick-btn slick-prev", "iconClass": "fas fa-angle-left" },
                                "nextArrow": {"buttonClass": "slick-btn slick-next", "iconClass": "fas fa-angle-right" }
                            }'
                         data-slick-responsive='[
                            {"breakpoint":991, "settings": {"slidesToShow": 2}},
                            {"breakpoint":690, "settings": {"slidesToShow": 2}},
                            {"breakpoint":590, "settings": {"slidesToShow": 1}}
                            ]'>

                        {% if carousel.imageWithLink %}
                            {% for block in carousel.imageWithLink.all() %}
                                <a href="{{ block.ahrefLink }}">
                                    <div class="carousle-slider text-center">
                                        <div class="thumb">
                                            {% for image in block.image.all() %}
                                                <img src="{{image.getUrl() }}" alt="{{image.imageAltTag }}">
                                            {% endfor %}
                                        </div>
                                    </div>
                                </a>
                            {% endfor %}
                        {% endif %}


                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- End Carousel Slider -->
{% endif %}
