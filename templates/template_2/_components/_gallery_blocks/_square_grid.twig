{% if gallery_block %}
    <div class="gallery-wrapper gallery-grid" id="animated-thumbnials2">
        {% for galleryImage in entry.galleryImages.all() %}

            <!-- Start Single Square Gallery -->
            <a class="gallery" href="{{galleryImage.getUrl()}}">
                <div class="thumb">
                    <img src="{{galleryImage.getUrl()}}" alt="{{ galleryImage.imageAltTag  }}">
                </div>
                <div class="hover-overlay">
                    <div class="inner">
                        <span class="ion ion-android-add"></span>
                    </div>
                </div>
            </a>
            <!-- End Single Square Gallery -->

        {% endfor %}
    </div>
{% endif %}